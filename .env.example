# Prisma database URL
# Local dev:
DATABASE_URL="file:./dev.db"
# EC2 production (persistent SQLite on disk):
# DATABASE_URL="file:./prisma/prod.db"

# Turso — only needed for Vercel/serverless; skip on EC2
TURSO_DATABASE_URL=""
TURSO_AUTH_TOKEN=""

# Budget login (set on EC2 in /var/www/adappt-site/.env)
BUDGET_ADMIN_EMAIL="hutsonosler@gmail.com"
# Locally escape each $ as \$ for Next.js. On EC2, paste hash without escapes.
BUDGET_ADMIN_PASSWORD_HASH="\$2b\$12\$..."

# Session signing secret
AUTH_SECRET=""
